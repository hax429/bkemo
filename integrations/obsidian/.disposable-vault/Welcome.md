# Disposable vault

Use this vault only for bkemo plugin O0 acceptance. Do not store real notes here.

After building the plugin, copy or symlink `../main.js`, `../manifest.json`, and
`../styles.css` into `.obsidian/plugins/bkemo/`.
