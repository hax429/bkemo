# Disposable vault

Use this vault only for **como** plugin acceptance. Do not store real notes here.

After building the plugin, run `../../scripts/build_ob.sh --dev` (installs into
this vault and `~/hax429`), or copy `../main.js`, `../manifest.json`, and
`../styles.css` into `.obsidian/plugins/como/`.
